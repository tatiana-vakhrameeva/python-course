#!/bin/bash

deco() {
    poetry run python src/deco.py
}

init() {
    echo "Init..."
    poetry env use python3.10
    poetry install
}

if [[ "$1" = "init" ]]; then
    init
elif [[ "$1" = "deco" ]]; then
    deco
fi
